/**
 * Find the kth-smallest element in the union of two sorted arrays in
 * O(log m + log n) where m and n are the sizes of the two arrays.
 *
 * @param m the first sorted array
 * @param n the second sorted array
 * @param k
 * @return the kth-smallest element in the union of m and n
 * @author Hendrik Werner
 */
int findKthSmallest(int[] m, int[] n, int k) {
	// if ?? return m[0]?? or n[0]??
	// compare some elements of m and n
	// throw away a fraction of both m and n
	// recursively call this with the reduced m and n
}
